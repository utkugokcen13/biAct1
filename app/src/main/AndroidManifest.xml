<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.utkugokcen.biact">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".settingspage"></activity>
        <activity android:name=".clubprofilepage" />
        <activity android:name=".myupcomingeventspage" />
        <activity android:name=".createeventpage" />
        <activity android:name=".createclubpage" />
        <activity android:name=".studentprofilepage" />
        <activity android:name=".clubfeedpage" />
        <activity android:name=".studentfeedpage" />
        <activity android:name=".signuppage" />
        <activity android:name=".loginpage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>